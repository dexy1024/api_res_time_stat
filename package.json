{
  "name": "video-studio",
  "private": true,
  "scripts": {
    "dev:web": "npm run dev --prefix apps/web",
    "dev:server": "npm run dev --prefix server",
    "build:web": "npm run build --prefix apps/web",
    "install:all": "npm install --prefix apps/web && npm install --prefix server"
  }
}
